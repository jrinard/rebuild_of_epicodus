<h1>Chapters</h1>

<ul>
  <% @chapters.each do |chapter| %>
    <li><%= link_to chapter.name, chapter_path(chapter) %> |
      <%= link_to "Edit", edit_chapter_path(chapter) %>
    </li> <!-- passed in chapter into path so it can find the id -->
  <% end %>
</ul><!-- Notice, too, that because we are in an each loop, we pass the object in as chapter instead of @chapter. -->


<hr>
<%= link_to "Chapters", chapters_path %> |
<%= link_to "New Chapter", new_chapter_path %>
